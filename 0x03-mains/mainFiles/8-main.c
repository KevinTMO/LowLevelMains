#include "roadmap.h"

/**
*main - will call printSquare() to print a square by n times using "#"
*Return: will return 0 if success
*/

int main(void)
{
	printSquare(0);
	printSquare(2);
	printSquare(10);
	printSquare(-4);
	return (0);
}