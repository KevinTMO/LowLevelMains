#include "roadmap.h"

/**
*main - will call printTriangle() to print a triangle
*Return: will return 0 if success
*/

int main(void)
{
	printTriangle(2);
	printTriangle(10);
	printTriangle(4);
	printTriangle(0);
	return (0);
}