#include "roadmap.h"

/**
*main - will call printLine() to print a line by n times using '_'
*Return: will return 0 if success
*/

int main(void)
{
	printLine(0);
	printLine(2);
	printLine(10);
	printLine(-4);
	return (0);
}