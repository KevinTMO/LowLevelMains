#include "roadmap.h"

/**
*main - will call printLastDigit to return the last digit of a number
*Return: will return 0 if success
*/

int main(void)
{
        printLastDigit(98);
        printLastDigit(-1024);
        printLastDigit(0);
        return (0);
}