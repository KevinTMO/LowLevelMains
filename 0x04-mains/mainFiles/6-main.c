#include "roadmap.h"

/**
*main - will call putByTwo() to print one character every two
*Return: will return 0 if success
*/

int main(void)
{
	char *str;

	str = "0123456789";
	putByTwo(str);
	return (0);
}